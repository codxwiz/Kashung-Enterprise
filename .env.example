# Public browser Pixel/Dataset ID (safe to expose)
NEXT_PUBLIC_META_PIXEL_ID=

# Cloudflare Worker secrets/configuration (never expose the access token publicly)
META_DATASET_ID=
META_ACCESS_TOKEN=
META_GRAPH_API_VERSION=vXX.X

# Optional: only while validating events in Meta Events Manager > Test events
META_TEST_EVENT_CODE=
