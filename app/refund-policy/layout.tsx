import { createPageMetadata } from "../site-metadata";
export const metadata = createPageMetadata({ title: "Refund Policy", description: "Payment, cancellation, revision, and refund terms for Kashung Enterprise projects.", path: "/refund-policy" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
