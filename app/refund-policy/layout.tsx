import type { Metadata } from "next";
export const metadata: Metadata = { title: "Refund Policy | Kashung Enterprise", description: "Payment, cancellation, revision, and refund terms for Kashung Enterprise projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
