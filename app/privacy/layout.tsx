import { createPageMetadata } from "../site-metadata";
export const metadata = createPageMetadata({ title: "Privacy Policy", description: "How Kashung Enterprise collects, uses, protects, and manages personal information.", path: "/privacy" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
